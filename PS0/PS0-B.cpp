#include <bits/stdc++.h>
using namespace std;

int main() {
    unsigned long A, B;
    while (1) {
        cin >> A >> B;
        if (A == 0 && B == 0) break;
        cout << A + B << endl;
    }
    return 0;
}